schema_version: 3

checks:
  envtest:
    files:
      envtest.py: file
      envtest/configuration.py: file
      envtest/contracts.py: file
      envtest/publication.py: file
      envtest/suite.py: file
    paths: [uv]
    commands:
      help: [python envtest.py --help, 'Run YAML-defined environment checks']
